import HeroTest from '@/components/HeroTest';

export const metadata = {
  title: 'Hero variants — test',
  robots: { index: false, follow: false },
};

export default function HeroTestPage() {
  return <HeroTest />;
}
